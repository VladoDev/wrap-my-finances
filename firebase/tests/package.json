{
  "name": "wrap-my-finances-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore Security Rules tests, run against the local emulator suite.",
  "type": "commonjs",
  "scripts": {
    "test": "mocha --timeout 20000 **/*.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.17.1",
    "mocha": "^11.1.0"
  }
}
